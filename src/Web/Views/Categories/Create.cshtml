@using ViewModels.Category
@using Core.Entities
@model CategoriesCreateInputModel
@{ 
    List<Category> existingCategories = new HashSet<Category>().ToList(); // TODO: display already existing categories
}

    <h1>Add Category </h1>

    <form method="post" enctype="multipart/form-data">
        <div asp-validation-summary="All"></div>

        <div class="form-group row">
            <label>Name</label>
            <input asp-for="@Model.Name" />
        </div>
        
        <input type="submit" class="btn btn-primary" />
    </form>
